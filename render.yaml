services:
  - type: web
    name: smart-crane-incident-review
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: ACECO_DEMO_MODE
        value: "true"
